// Tabler ships its own JS (Bootstrap + plugins); rapyd's core already brings
// Bootstrap, TomSelect, modals, the theme switcher and livewire-sortable.
import '@rapyd/js/rapyd-core';
import '../sass/theme.scss';
